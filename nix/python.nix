# Python project spec
{
  # Parent-only fields
  emptyRoot = false;        # Root workspace contains a Python package (unused for now)
  workspaces = [];          # No additional workspaces (unused for now)
  
  # Project spec fields  
  projectName = "template";
  projectRoot = ".";
  # projectDir is optional - if not defined, defaults to "src/<sanitizedName>"
}
